class AppConstants {
  AppConstants._();
  static const double appBarIconWidth = 24;
  static const double appBarIconHeight = 24;
  static const String appVersion = "1.0.0";

  /// Display name used in share text and branding.
  static const String appName = 'The Message of The Quran';

  /// Store URLs appended to ayah share text; empty until published.
  static const String androidStoreUrl = '';
  static const String iosStoreUrl = '';
}
